# GitHub Models API Token (REQUIRED for AI extraction)
# Get token from: https://github.com/settings/tokens
# Required scopes: None (public models access)
# 
# This token enables AI-powered extraction of lab report data.
# Without this token, the system cannot extract clinical data from reports.
#
# To use:
# 1. Create a GitHub personal access token
# 2. Copy .env.example to .env
# 3. Replace the value below with your actual token
#
VITE_GITHUB_TOKEN=your_github_token_here
