# GitHub Models API Token (optional)
# Get token from: https://github.com/settings/tokens
# Required scopes: None (public models access)
# Used for LLM-assisted structure extraction (fallback if deterministic parsing fails)
VITE_GITHUB_TOKEN=your_github_token_here
